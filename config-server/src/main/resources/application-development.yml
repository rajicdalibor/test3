server.port: 8001

spring:
  application.name: config-server
  cloud.config.server.git:
    uri: file://${user.home}/Projects/3ap/3APAdmin

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    registryFetchIntervalSeconds: 1
  instance:
    leaseRenewalIntervalInSeconds: 1
